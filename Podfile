# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CouchbaseProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CouchbaseProject
	pod 'couchbase-lite-ios', '~> 1.4'
    pod 'Kingfisher'
    pod "SwViewCapture"
    pod 'Alamofire', '~> 4.5'
    pod 'AlamofireObjectMapper', '~> 5.0'
    pod 'PromiseKit', '~> 4.0'

  target 'CouchbaseProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CouchbaseProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
